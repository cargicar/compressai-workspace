# Config for bmshj2018_compression.py exclusively (the pretrained CompressAI zoo baseline).
# Only cfg['data']['h5_path'/'field_key'/'timestep'] are ever read by that script — no svd:,
# model:, or training: sections, and no patch_size (the codec slices 2D planes on the fly,
# sized via --axis/--batch-size CLI flags instead).

data:
  #h5_path:    "/data/mldc/pde_bench/Turb_M1.hdf5"
  h5_path:     "/home/carlos/Rnet_local/datasets/pde_bench/Turb_M1.hdf5" #Alienware local
  field_key:  "pressure"
  timestep:   40       # index into the nt dimension (0-based → physical t=16)
